<%= form_with(model: cate_blog) do |form| %>
  <% if cate_blog.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(cate_blog.errors.count, "error") %> prohibited this cate_blog from being saved:</h2>

      <ul>
        <% cate_blog.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
